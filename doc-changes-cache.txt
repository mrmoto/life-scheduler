# Documentation Audit - Issues Found
# Format: filename, line, [br], <30 word description>
# Legend: [br] = needs broken/rewritten, [miss] = missing content, [wrong] = factually incorrect, [align] = misaligned with principles

## CONTEXT.md Audit (Lines 1-353)

CONTEXT.md, 98-136, [miss], Container Services section shows structure but NEVER specifies Dockerfile MUST contain git, build-essential, postgresql-client, vim/nano. Critical gap: docs assume correct Dockerfile without defining requirements.

CONTEXT.md, 111-136, [miss], Development Environment section explains Dev Containers concept but doesn't mandate complete development toolchain in container image. Missing: "Dockerfile requirements for complete dev environment."

CONTEXT.md, 138-179, [wrong], Directory structure shows docs/ at root but actual location is mnt/project/docs/agent-training-(link)/. Path mismatch breaks references.

CONTEXT.md, 152-153, [miss], Dockerfile listed but no specification of REQUIRED contents (system packages, build tools, dev utilities). Container-first violated if Dockerfile incomplete.

CONTEXT.md, 208, [wrong], "Container Immutability: Production images read-only" but dev containers need git for commits. Confuses prod vs dev container requirements. Needs split explanation.

CONTEXT.md, 210, [align], Dev Containers rule exists but doesn't connect to "Dockerfile MUST include complete dev tooling." Says what (use Dev Containers) not how (Dockerfile requirements).

CONTEXT.md, 254-255, [wrong], References "/docs/mcp/CONTEXT.MCP.md" but actual path is "/mnt/project/docs/agent-training-(link)/CONTEXT.MCP.md". All MCP doc paths incorrect.

## DECISIONS.md Audit (Lines 1-374)

DECISIONS.md, 132-157, [miss], ADR-005 Container-First Development specifies USING Docker but NEVER specifies Dockerfile MUST contain complete dev toolchain (git, build-essential, postgresql-client, editors). ROOT CAUSE of M1 incompleteness.

DECISIONS.md, 141-146, [miss], Rationale lists benefits of containers but doesn't mandate "Dockerfile requirements: git, build tools, shell utilities, text editors for complete dev environment." Philosophy without implementation specs.

DECISIONS.md, 299-331, [miss], ADR-011 Dev Containers Mandatory says WHERE to develop (inside container) but not WHAT container needs (git for commits, build-essential for compilations, vim/nano for editing, psql for debugging).

DECISIONS.md, 308-315, [miss], Rationale explains WHY Dev Containers but missing "Implementation: Dockerfile MUST include system packages beyond Python - git (version control), build-essential (C extensions), postgresql-client (psql), text editors (vim/nano)."

DECISIONS.md, 316-324, [miss], Consequences lists benefits but doesn't specify "Container must be complete: cannot commit without git, cannot compile packages without build tools, cannot debug DB without psql client."

DECISIONS.md, 326-330, [align], Alternatives section rejects local venv (correct) but doesn't explain "rejected because requires complete container with all dev tools, not minimal Python-only image."

## CURSOR_TASKS.md Audit (Lines 1-632)

CURSOR_TASKS.md, 78-115, [br], M1-Dockerfile task is ROOT CAUSE of broken container. Specifies minimal Python-only image, NEVER mentions git, build-essential, postgresql-client, vim/nano. Creates incomplete dev environment.

CURSOR_TASKS.md, 90-105, [miss], Dockerfile spec missing CRITICAL requirements: "RUN apt-get install git build-essential postgresql-client vim curl" before Python setup. Without these, container cannot git commit, compile packages, or debug DB.

CURSOR_TASKS.md, 107-111, [wrong], "Requirements" section focuses on multi-stage build and image size but OMITS complete dev toolchain. Should prioritize "complete development environment" over "image size optimization."

CURSOR_TASKS.md, 117-120, [miss], Success criteria checks build success and size but NOT "can run git commit, can compile psycopg2 from source, can run psql, can edit files with vim."

CURSOR_TASKS.md, 60-62, [wrong], References "/mnt/project/docs/" paths - correct. But CONTEXT.md and DECISIONS.md don't specify Dockerfile requirements anyway (cascading error).

CURSOR_TASKS.md, 391-459, [miss], M1-DevContainer task configures IDE attachment but NEVER mentions prerequisite: "Dockerfile MUST include git, build-essential, postgresql-client, editors BEFORE Dev Containers will work for complete development."

CURSOR_TASKS.md, 405, [align], Correctly states "NOT creating separate dev container" (single source of truth) but doesn't specify "existing app service MUST have complete dev toolchain installed."

CURSOR_TASKS.md, 450-458, [miss], Success criteria tests Python tools (pytest, Flask) but OMITS: "git commit works, C extensions compile, psql connects to db, vim/nano available for editing."

CURSOR_TASKS.md, 445-447, [wrong], References ADR-011 and CONTEXT.md but those docs also incomplete (don't specify Dockerfile requirements). Circular reference to incomplete docs.

## ENFORCEMENT.md Audit (Lines 1-470)

ENFORCEMENT.md, 281-305, [miss], Rejection script for "create venv locally" correctly redirects to Dev Containers but NEVER specifies "container MUST include git, build-essential, postgresql-client, editors for complete dev environment."

ENFORCEMENT.md, 297-302, [miss], "Correct approach" lists steps (use Dev Containers, reopen in container) but missing prerequisite: "First verify Dockerfile contains complete dev toolchain (git, build tools, DB client, editors)."

ENFORCEMENT.md, 307-314, [miss], Detection triggers catch "create venv" and "install Python locally" but MISSING triggers for incomplete containers: "We don't need git in container", "Just use host git", "Image size optimization over completeness."

## MILESTONES.md Audit (Lines 1-293)

MILESTONES.md, 27-40, [miss], M1 success criteria checks "docker compose up works" and "Dev Containers configured" but NEVER verifies "Dockerfile contains git, build-essential, postgresql-client, editors for complete dev environment."

MILESTONES.md, 28, [wrong], Checkbox marked complete for "docker compose up starts successfully" but underlying Dockerfile incomplete (missing dev toolchain). False completion signal.

MILESTONES.md, 36-38, [miss], Dev Containers criteria test IDE attachment and Python tools but OMIT: "can git commit from container, can compile C extensions, can psql debug, can vim edit."

MILESTONES.md, 67, [wrong], Deliverable "Dockerfile (multi-stage build)" marked complete but Dockerfile lacks git, build-essential, postgresql-client, vim/nano. Incomplete deliverable marked complete.

MILESTONES.md, 90-96, [miss], Critical Gap section identifies Dev Containers missing but DOESN'T identify "Dockerfile incomplete (no git/build-essential)" as ROOT technical issue. Symptom identified, not cause.

MILESTONES.md, 92, [wrong], "Root cause: ADR-005 didn't explicitly mandate `.devcontainer/` configuration" - WRONG. Root cause is ADR-005 didn't mandate COMPLETE Dockerfile with dev toolchain. Misdiagnosed root cause.

## *backpropagation-002.md Audit (Lines 1-120)

*backpropagation-002.md, 42-89, [wrong], "Dev Containers Missing from M1" entry identifies symptom (.devcontainer/ missing) but NOT root cause (Dockerfile incomplete - no git, build-essential, postgresql-client, editors). Will train future MCPs incorrectly.

*backpropagation-002.md, 46-47, [wrong], "Root Cause: ADR-005 didn't mandate Dev Containers" - INCORRECT. TRUE root cause: ADR-005 never specified Dockerfile MUST contain complete dev toolchain (git, build tools, DB client, editors).

*backpropagation-002.md, 52-55, [miss], "What Was Missing" lists .devcontainer/ config but OMITS critical gap: "Dockerfile missing system packages: git (commits), build-essential (compilation), postgresql-client (debugging), vim/nano (editing)."

*backpropagation-002.md, 63-67, [miss], "What We Actually Need" correctly says ONE container stack but never specifies "Dockerfile MUST include: apt-get install git build-essential postgresql-client vim curl before Python setup."

*backpropagation-002.md, 69-74, [miss], "Lesson for God" teaches "Dev Containers Mandatory" but OMITS lesson: "Dockerfile MUST be complete dev environment, not minimal Python-only image. Container-First means ALL dev tools in container."

*backpropagation-002.md, 84-88, [miss], "Action for Future MCPs" says verify .devcontainer/ exists and pytest runs but DOESN'T say "verify: git commit works, C extensions compile, psql connects, vim edits. Test COMPLETE dev environment."

## CONTEXT.MCP.md Audit (Lines 1-386)

CONTEXT.MCP.md, 98-100, [wrong], References paths "/mnt/project/docs/CONTEXT.md" etc without "agent-training-(link)" subdirectory. Should be "/mnt/project/docs/agent-training-(link)/CONTEXT.md". Path errors throughout.

CONTEXT.MCP.md, 148, [wrong], References "/mnt/project/docs/mcp/CURSOR_TASKS.md" but correct path is "/mnt/project/docs/agent-training-(link)/CURSOR_TASKS.md". Misdirection for MCPs trying to find tasks.

CONTEXT.MCP.md, 180-183, [miss], "Review Milestone Completions" says verify success criteria met but NEVER specifies "verify Dockerfile has complete dev toolchain (git, build-essential, postgresql-client, editors)." Can't verify what's not defined.

CONTEXT.MCP.md, 200-203, [miss], "Don't write Dockerfile syntax" (correct) but MCP SHOULD verify Dockerfile completeness. Missing responsibility: "DO verify Dockerfile contains all required dev tools before accepting M1 completion."

CONTEXT.MCP.md, 236-244, [miss], Enforcement rule lists shortcuts to reject but OMITS: "Minimal container is fine", "We don't need git in container", "Image size over completeness", "Use host git."

CONTEXT.MCP.md, 265, [wrong], References "/mnt/project/docs/mcp/ENFORCEMENT.md" but actual path includes "agent-training-(link)". Path error prevents MCPs finding enforcement scripts.

CONTEXT.MCP.md, 322-332, [wrong], All template paths reference "/mnt/project/docs/mcp/" but correct structure is "/mnt/project/docs/agent-training-(link)/". Breaks builder/advisor spawning.

CONTEXT.MCP.md, 343-346, [wrong], Check Documentation commands show wrong paths (missing "agent-training-(link)" subdirectory). MCPs will fail to find actual documentation files.

CONTEXT.MCP.md, 352, [wrong], References "/mnt/project/docs/mcp/MODELS.md" with incorrect path structure.

## SPAWN.BUILDER.md Audit (Lines 1-248)

SPAWN.BUILDER.md, 16, [wrong], References "/mnt/project/docs/mcp/CURSOR_TASKS.md" but correct path is "/mnt/project/docs/agent-training-(link)/CURSOR_TASKS.md". Misdirects MCP trying to find tasks.

SPAWN.BUILDER.md, 31, [wrong], Same path error: "/mnt/project/docs/mcp/CURSOR_TASKS.md" missing "agent-training-(link)" subdirectory.

SPAWN.BUILDER.md, 70-73, [miss], "Review Against Success Criteria" says check best practices but DOESN'T specify checking Dockerfile completeness: "Verify container has git, build-essential, postgresql-client, editors if M1 task."

SPAWN.BUILDER.md, 136-146, [miss], M1 task list includes Dockerfile, Docker-Compose but NEVER notes "Dockerfile MUST include complete dev toolchain (git, build tools, DB client, editors)." MCP can't verify what's not specified.

SPAWN.BUILDER.md, 139, [miss], "M1-Dockerfile: Multi-stage production image" - misleading. Should say "Complete development + production image with full toolchain." Current description implies minimal.

SPAWN.BUILDER.md, 202-209, [miss], Anti-patterns list catches common shortcuts but OMITS: "Minimal container is fine", "We don't need git in container", "Image size over completeness."

SPAWN.BUILDER.md, 234, [wrong], References "/mnt/project/docs/mcp/ENFORCEMENT.md" with incorrect path (missing "agent-training-(link)").

SPAWN.BUILDER.md, 242, [wrong], References "/mnt/project/docs/mcp/MODELS.md" with incorrect path structure.

## SPAWN.ADVISOR.md Audit (Lines 1-247)

SPAWN.ADVISOR.md, 18-20, [wrong], References "/mnt/project/docs/CONTEXT.md" etc without "agent-training-(link)" subdirectory. Should be "/mnt/project/docs/agent-training-(link)/CONTEXT.md". Path errors.

SPAWN.ADVISOR.md, 192, [wrong], References "/mnt/project/docs/mcp/MODELS.md" but correct path includes "agent-training-(link)" subdirectory.

SPAWN.ADVISOR.md, 209, [wrong], Template shows "/mnt/project/docs/DECISIONS.md" without "agent-training-(link)" path component.

SPAWN.ADVISOR.md, NO ISSUES, Document focused on quick Q&A, no Dockerfile/container completeness issues relevant here. Path errors only.

## MCP_HANDOFF.md Audit (Lines 1-63)

MCP_HANDOFF.md, 26-34, [miss], Session MCP-001 logs framework setup but DOESN'T note "CRITICAL ISSUE: M1 tasks don't specify complete Dockerfile requirements (git, build-essential, etc). Must fix before M1 completion."

MCP_HANDOFF.md, NO OTHER ISSUES, File is handoff log template only. No technical spec errors. Missing note about known Dockerfile gap.

## MODELS.md Audit (Lines 1-255)

MODELS.md, NO CRITICAL ISSUES, Document about model selection for different agent roles. No Dockerfile/container completeness issues. Content appears aligned with project needs.

## README.md (agent-training) Audit (Lines 1-426)

README.md, 62, [wrong], References "/docs/mcp/CURSOR_TASKS.md" but correct path is "/mnt/project/docs/agent-training-(link)/CURSOR_TASKS.md". Path misdirection for Scott.

README.md, 129, [wrong], File references "CONTEXT_MCP.md" but actual filename is "CONTEXT.MCP.md" (with period, not underscore).

README.md, 287-289, [wrong], Refer to docs paths show "/mnt/project/CONTEXT.md" but actual paths include "agent-training-(link)" subdirectory.

README.md, 357-369, [wrong], File Locations section shows "/mnt/project/CONTEXT.md" etc without "agent-training-(link)" subdirectory. Will confuse Scott when looking for files.

README.md, 186-196, [miss], "The 8 Laws" lists non-negotiable rules but OMITS #9 (Automated Solutions First) and #10 (Dev Containers Only). Should list all 10 non-negotiables from CONTEXT.md.

README.md, NO DOCKERFILE ISSUES, Quick reference doc, paths are main issue. Content doesn't specify Dockerfile requirements (appropriate for quick reference).

## mcp-backpropagation-001.md Audit (Lines 1-311)

mcp-backpropagation-001.md, NO CRITICAL ISSUES, Historical document about MCP role discovery. No Dockerfile/container completeness issues. Document is retrospective analysis, not specification.

## ==============================================================================
## AUDIT SUMMARY - SYSTEMIC FAILURES IDENTIFIED
## ==============================================================================

### ROOT CAUSE (The Original Sin)
ADR-005 (Container-First Development) and ADR-011 (Dev Containers Mandatory) specify PHILOSOPHY but NEVER define IMPLEMENTATION REQUIREMENTS for Dockerfile.

MISSING FROM ALL DOCUMENTATION:
"Dockerfile MUST contain complete development toolchain:
- git (version control, commits inside container)
- build-essential (gcc, make - for C extensions, psycopg2 compilation)
- postgresql-client (psql CLI for database debugging)
- vim AND nano (text editing inside container)
- curl (already present, health checks + API testing)"

### CASCADE EFFECT (How One Gap Broke Everything)
1. ADR-005/ADR-011 lack Dockerfile specs → 
2. CURSOR_TASKS.md M1-Dockerfile task creates minimal Python-only image →
3. MILESTONES.md success criteria never check for complete toolchain →
4. ENFORCEMENT.md never flags "minimal container" as anti-pattern →
5. *backpropagation-002.md misdiagnoses root cause (symptom vs cause) →
6. Future MCPs inherit incorrect understanding →
7. M1 marked "complete" despite broken foundation

### SECONDARY ISSUE (Path Errors Throughout)
Nearly every document references paths as "/mnt/project/docs/" when actual structure is "/mnt/project/docs/agent-training-(link)/". This breaks:
- MCP file lookups
- Cursor task references  
- Scott's manual navigation
- Automated tooling (if built)

### DOCUMENTS WITH CRITICAL FLAWS (Require Rewrites)
1. DECISIONS.md (ADR-005, ADR-011) - Missing implementation specs
2. CURSOR_TASKS.md (M1-Dockerfile task) - Creates broken container
3. MILESTONES.md (M1 success criteria) - False completion signals
4. *backpropagation-002.md - Incorrect root cause diagnosis
5. ENFORCEMENT.md - Missing "minimal container" detection triggers

### DOCUMENTS WITH PATH ERRORS ONLY (Fixable with Find/Replace)
1. CONTEXT.md
2. CONTEXT.MCP.md  
3. SPAWN.BUILDER.md
4. SPAWN.ADVISOR.md
5. README.md (agent-training)

### DOCUMENTS WITH NO CRITICAL ISSUES
1. MODELS.md (model selection guide, unrelated to Dockerfile)
2. mcp-backpropagation-001.md (historical document)
3. MCP_HANDOFF.md (template only, one missing note)

### TOTAL ISSUES FOUND: 61 across 13 documents
- [miss] Missing content: 39 issues
- [wrong] Factually incorrect: 17 issues  
- [br] Needs rewrite: 5 issues

### IMPACT ASSESSMENT
**Project is NOT salvageable by fixing documentation alone.**

The actual Dockerfile in the repository is ALREADY BROKEN (missing dev toolchain). Documentation fixes won't retroactively fix the container that was built.

REQUIRED ACTIONS:
1. Rewrite Dockerfile with complete dev toolchain
2. Rebuild container image
3. Test: git commit, psycopg2 compile, psql connect, vim edit - ALL inside container
4. Fix 61 documentation issues
5. Create new M1 completion checklist that verifies COMPLETE dev environment
6. Only then can M1 be marked complete

This is the "layer of shit on layer of shit" Scott described.


