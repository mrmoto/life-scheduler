# =============================================================================
# Life Scheduler - Runtime Dependencies
# =============================================================================
# Python packages required for the application to run
# All versions pinned for reproducibility
# Dev-only tools are in requirements-dev.txt

# Web Framework
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.1

# Database
SQLAlchemy==2.0.23
psycopg2-binary==2.9.9  # PostgreSQL adapter (binary for easier install)
alembic==1.13.1         # Database migrations

# Environment
python-dotenv==1.0.0    # Load .env files
